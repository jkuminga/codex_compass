#!/bin/sh
set -eu

script_directory=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
project_root=$(CDPATH= cd -- "$script_directory/.." && pwd)
cd "$project_root"
exec python3 -m src.harness.hooks.pre_tool_use_guard \
  --timeout 7 \
  -- uv run python -m src.harness.hooks.pre_tool_use
