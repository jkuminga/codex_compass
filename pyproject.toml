[project]
name = "harness-v2"
version = "0.1.0"
description = "Local project-state harness and Codex MCP tools"
requires-python = ">=3.10"
dependencies = [
  "mcp>=2,<3",
]

[dependency-groups]
dev = []
