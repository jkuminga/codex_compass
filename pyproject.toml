[project]
name = "harness-v2"
version = "0.1.0"
description = "Local project-state harness and Codex MCP tools"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.116,<1",
  "mcp>=2,<3",
  "uvicorn>=0.35,<1",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
]
